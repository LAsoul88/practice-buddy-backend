import users from './users'
import entries from './entries'
import auth from './auth'

const routesPathMap = {
  '/users': users,
  '/entries': entries,
  '/auth': auth
}

export default routesPathMap